{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd packages/research && pnpm install && pnpm --filter @peptalk/schemas build && pnpm --filter @peptalk/database build && pnpm build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd packages/research && pnpm exec tsx server.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
