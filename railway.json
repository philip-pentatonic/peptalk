{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @peptalk/schemas build && pnpm --filter @peptalk/database build && pnpm --filter @peptalk/research build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd packages/research && pnpm exec tsx server.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
